- [ ] zsh aufräumen: PATH & Tool-Init (Homebrew, nvm, pyenv) → `~/.zshenv` (wird immer geladen, auch non-interactive); `~/.zshrc` nur interaktiv (Aliasse, Prompt, Completions, Plugins). Fixt „Claude Code findet mein Tool nicht".
- [ ] sketchybar: move workspaces indicator to its current screen when moving workspaces to a diferent indicator
- [ ] sketchybar: investigate accumulating memory usage (higher for longer uptimes)
- [ ] sketchybar: build ai session limit indicator (seesion+weekly) for claude (and then other providers) and show when there is an active session window
- [ ] karabiner: shortcuts concept ausdenken: alt-left -> hyperkey ctrl-left -> own custom per-app shortcuts, etc.
- [ ] sketchybar: fix that the battery indicator no longer shows the icon (only shows percentage)
- [ ] tmux: install and set up tmux; launch processes like sketchybar inside them (enables viewing of the log)
- [ ] represent karabiner config using typescript generator (eg. to easily add new apps to the exception handling)

